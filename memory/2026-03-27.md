# 2026-03-27

- Built and iteratively extended a QQ bot project at `/root/.openclaw/workspace/qq-bot-3371871293` using NapCat + NoneBot2 + OneBot v11 for QQ `3371871293`.
- NapCat/QQ setup was brought up on this host; the bot runs via screen session `qqbot`. Avoid storing tokens/keys in git; `.env` is intentionally ignored.
- Core bot features now present: help/ping/time/echo, MiniMax text-to-image, MiniMax TTS read-aloud (`朗读` / `念`), and two-step image-to-image flow (`图生图 提示词` then send image separately).
- Important reliability fix: `bot.py` now explicitly loads the project-local `.env` and startup uses `cd /root/.openclaw/workspace/qq-bot-3371871293` so runtime config is actually applied.
- MiniMax text generation for this user key works with `MiniMax-M2.7`; some other MiniMax text/music models were unsupported by the token plan. Music generation was tested and found unsupported by the current key/plan.
- Group-chat “roaster” behavior was added: it tracks recent group context and every ~5-10 messages may generate a short contextual, aggressive/ironic forum-style reply with MiniMax. It skips bot command messages.
- Bilibili link handling was added for QQ share cards and b23 links. Current design: extract b23/Bilibili URL from QQ JSON card, resolve to BV, fetch Bilibili video details via official web API, fetch subtitles if available, fetch hot comments, then ask MiniMax to produce a short content-based “贴吧老哥” style reply.
- End-of-session status: Bilibili pipeline now successfully reaches real video metadata/subtitles/comments, and generic fallback replies were removed. However, the user still felt replies were too generic/repetitive, so the next step should be to improve the Bilibili reply quality by first summarizing/interpreting video content (possibly with an intermediate internal summary step) before generating the final roast.
- User preference established: for Bilibili replies, do NOT rely on group chat context; base the response on the actual video content/comments instead, and make replies concrete rather than generic.
