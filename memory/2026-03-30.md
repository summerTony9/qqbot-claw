- Handled a large company-analysis batch from user-provided Excel (艺璇 batch, 123 companies). Generated per-company markdown reports under `yixuan-batch/companies/`, created a separate hyperlinked index, and coordinated many subagent runs to fill missing analyses after discovering the initial company list file had been contaminated by old data.
- Important repo lesson: workspace `origin` was preconfigured to `git@github.com-qqbot:summerTony9/qqbot-claw.git`, which caused an accidental push to the wrong repo. User intended delivery to `summerTony9/company-info`.
- Recovered correct push workflow for `company-info`: previous working setup was in `obsidian-vault/.git/config`, using HTTPS + PAT for `https://github.com/summerTony9/company-info.git`. SSH config only had `github.com-qqbot` key alias, so direct SSH push to `company-info` failed.
- Mistake made: force-pushed incorrect content to `company-info`, then user provided backup zips. Restored repository contents by merging backup with new batch output, reverted mistaken commit on `qqbot-claw` back to `ed7f14a`, and repushed `company-info` cleanly.
- Final `company-info` structure restored/pushed:
  - `第一批_49户/`
  - `第二批_104户/`
  - `艺璇批次_123户/`
  - corresponding index files, with 艺璇 batch index rebuilt as per-company clickable markdown links.
- User preference: when syncing analysis results to GitHub, do NOT push raw/search/cache files; push only intended markdown deliverables. Also avoid wrong repo pushes and avoid showing commits as `penggaolai` if possible.
- User supplied a PAT directly in chat and preferred “方案1” (their own token / desired identity path) to avoid pushes appearing as `penggaolai`. For later repair, used git author config `summerTony9 <summerTony9@users.noreply.github.com>` for the restoration commit.
- User explicitly wants anti-block / anti-rate-limit search tactics remembered for future company-research batches: batch searches in groups of 10-20 companies, use 5-20s randomized pauses between queries, pause 3-10 minutes between batches, vary keyword patterns, distribute searches across multiple sources (Baidu/Bing/Sogou/enterprise databases/public procurement/official sites), avoid repetitive same-sequence search behavior, click through to read pages instead of search-only behavior, prefer logged-in/local browser or residential-like environment when possible, do rough screening first and only deep-dive A/B prospects, and stop/switch source immediately when captcha/risk-control appears.
- User additionally wants browser-relay/local-browser fallback included in the method: if server-side querying is blocked, low-quality, or persistently fails, use the user's local Chromium via relay as a fallback/priority path for research, since requests originate from the user's local IP/browser environment.
- User asked that these lessons be remembered along with git/push lessons.
